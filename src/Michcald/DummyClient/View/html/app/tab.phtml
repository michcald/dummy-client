<?
$items = array(
    array(
        'label' => 'Read',
        'active' => $_['active'] == 'read',
        'icon' => 'book',
        'url' => $this->url('dummy_client.app.read', array(
            'id' => $_['app']->getId()
        ))
    ),
    array(
        'label' => 'Update',
        'active' => $_['active'] == 'update',
        'icon' => 'edit',
        'url' => $this->url('dummy_client.app.update', array(
            'id' => $_['app']->getId()
        ))
    ),
    array(
        'label' => 'Delete',
        'active' => $_['active'] == 'delete',
        'icon' => 'trash',
        'url' => $this->url('dummy_client.app.delete', array(
            'id' => $_['app']->getId()
        ))
    ),
    array(
        'label' => 'Grants',
        'active' => $_['active'] == 'grants',
        'icon' => 'ok',
        'url' => $this->url('dummy_client.grant.index', array(
            'appId' => $_['app']->getId()
        ))
    ),
);
?>

<ul class="nav nav-tabs" role="tablist">
    <? foreach ($items as $item): ?>
    <li<? if($item['active'] == 'read'): ?> class="active"<? endif; ?>>
        <a href="<?= $item['url'] ?>">
            <span class="glyphicon glyphicon-<?= $item['icon'] ?>"></span>
            <?= $item['label'] ?>
        </a>
    </li>
    <? endforeach; ?>
</ul>

<br />